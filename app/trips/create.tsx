import { Text, View } from 'react-native';

export default function CreateTripScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center', padding: 24 }}>
      <Text>Trip erstellen folgt als Nächstes.</Text>
    </View>
  );
}